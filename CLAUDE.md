# mikan-counter — 前提メモ

温州みかんの動画から果実を数える、卒業研究のプロトタイプ（PC版）。
セッションをまたいで前提がぶれないように、背景をここに残す。

## 対象と背景

- 対象は**温州みかん**。果実の見た目が生育ステージで大きく変わる。
  - **緑果（摘果期・8〜9月）**: 着色前で緑色。葉とほぼ同色でコントラストが小さい。
    摘果作業の判断に着果数が要るので、**こちらが主用途**。
  - **着色果（収穫期）**: 黄〜オレンジに着色済み。葉との差が大きく、検出は相対的に容易。
- この 2 条件は難易度がまったく違うので、アプリは**両方を扱い、条件を切り替えて比較できる**ことを最優先にする。
- 緑果では色しきい値や Hough 円検出が使えない（先行研究で緑カンキツは 色+テクスチャ 75.3%、形状+Hough円 80.4% にとどまる）。
  したがって検出は**学習ベース（YOLO）**で行う。
- 手元にあるのは木10本ほどを撮った**動画のみ**。緑果の学習済みモデルはまだない。

## やらないこと（重要）

- **収量を予測しない。** 収穫実測値（正解データ）がないため、予測モデルは成立しない。
  このアプリは「動画に写っている果実を数える」だけ。
- **UI に「収量」「予測」という語を出さない。**
- MVP の機能を勝手に増やさない。

## 期待される挙動

- 素の COCO 事前学習モデルは、**着色果ならある程度検出できるが、緑果ではほぼ何も検出できない**。
  これが正しい挙動であり、**この差そのものが卒論のベースライン図になる**。
- 検出0件はエラーにせず「検出0件」と表示し、緑果を選んでいるときは自前学習への案内を出す。

## 実装上の約束

- `src/` は Streamlit に依存させない（`detector.py` / `counting.py` は単体テスト可能な純粋関数）。UI は `app.py` だけ。
  後でスマホアプリへ移植するため。
- 動画は数百MBになり得るので全フレームをメモリに載せない。`cv2.VideoCapture` で逐次読み、間引いた分だけ処理する。
- デバイスは `mps` → `cuda` → `cpu` の順で自動選択（開発機は Apple Silicon の Mac）。
- モデルは `@st.cache_resource`、処理結果は `st.session_state`（スライダー操作で再処理しない）。
- `detector.py` はステージを引数として受け取る（`model_path_for_stage`）。今はモデル選択に影響しないが、
  将来ステージごとに別モデルを割り当てられるようにするため。
- 主指標は**フレームあたり最大検出数**。追跡のユニークID数は「二重カウント除去は未検証の参考値」と必ず注記する。
- アノテーションのクラスは `green_fruit` / `colored_fruit` の 2 クラス（統合はできても分割はできないため）。
- UI表記は日本語。コード内のコメント・変数名は英語でよい。
- 最終的にはスマホアプリにするが、今回はその前段の PC 版。
