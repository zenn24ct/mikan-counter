# ローカル（macOS / Apple Silicon）と Streamlit Community Cloud（Linux）の両方で
# 動作確認したバージョン。Python 3.12 前提。
#
# Linux では torch を CPU 版に固定する。既定の PyPI 版は CUDA 同梱で数GBあり、
# Community Cloud のディスク・メモリに収まらずデプロイが失敗するため。
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.13.0+cpu; sys_platform == "linux"
torchvision==0.28.0+cpu; sys_platform == "linux"
torch==2.13.0; sys_platform == "darwin"
torchvision==0.28.0; sys_platform == "darwin"

ultralytics==8.4.135
streamlit==1.62.0
opencv-python-headless==5.0.0.93
numpy==2.5.2
pandas==3.0.5
lap==0.5.13        # 追跡（ByteTrack）に必要

# 注意: ultralytics は依存として opencv-python（非headless）を引き込みます。
# ローカルでは Streamlit と競合するので、インストール後に headless 版へ戻してください:
#   pip uninstall -y opencv-python
#   pip install --force-reinstall --no-deps opencv-python-headless==5.0.0.93
# Community Cloud 側は packages.txt で libgl1 を入れて対処しています。
